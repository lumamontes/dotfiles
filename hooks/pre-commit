#!/usr/bin/env bash
set -euo pipefail
if ! command -v gitleaks >/dev/null 2>&1; then
  echo "pre-commit: gitleaks ausente — rode 'brew install gitleaks'" >&2
  exit 1
fi
gitleaks git --staged --redact --config .gitleaks.toml
